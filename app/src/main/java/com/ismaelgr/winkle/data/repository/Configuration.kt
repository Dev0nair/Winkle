package com.ismaelgr.winkle.data.repository

object Configuration {

    internal val ACCOUNT_USAGE = RepositoryOptions.NETWORK
    internal val PROFILE_USAGE = RepositoryOptions.NETWORK
    internal val PRODUCTS_USAGE = RepositoryOptions.NETWORK
    internal val CESTA_USAGE = RepositoryOptions.NETWORK
    internal val REPORTS_USAGE = RepositoryOptions.NETWORK
    internal val RATE_USAGE = RepositoryOptions.NETWORK
    internal val STORAGE_USAGE = RepositoryOptions.NETWORK

    internal val MAX_RATE = 5
}